// This page is no longer used.
// The root of the authenticated app is now handled by src/app/(app)/page.tsx
// Unauthenticated users are redirected by the AuthContext within the (app) layout or individual pages.
export default function RootPageRemoved() {
  return null;
}
